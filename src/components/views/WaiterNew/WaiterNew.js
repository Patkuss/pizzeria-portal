import React from 'react';
import styles from './WaiterNew.module.scss';

const WaiterNew = () => (
  <div className={styles.component}>
    <h2>WaiterNew view</h2>
  </div>
);

export default WaiterNew;
