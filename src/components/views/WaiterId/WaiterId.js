import React from 'react';
import styles from './WaiterId.module.scss';

const WaiterId = () => (
  <div className={styles.component}>
    <h2>WaiterId view</h2>
  </div>
);

export default WaiterId;
